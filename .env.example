# Copy to a local .env file if your tooling loads environment files.
# Do not commit real secrets.

SENTRY_ENV=development
SENTRY_AUDIT_HMAC_KEY=replace-with-32-byte-random-hex
SENTRY_MESH_SIGN_KEY=replace-with-32-byte-random-hex
